#include "main.h"

/**
 *_abs - function
 *
 *Description: it prints the absolute value of a given number
 *
 *Return: the absolute value of the number
 *
 *@n: the number
*/

int _abs(int n)
{
if (n < 0)
return (-n);
else
return (n);
}
